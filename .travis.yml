language: cpp
dist: xenial
osx_image: xcode10.1

matrix:
  include:
    - name: Linux
      os: linux
      compiler: gcc
      addons:
        apt:
          packages:
            [scons, pkg-config, build-essential, p7zip-full]
      env: PLATFORM=x11 ADDITIONAL_FLAGS="tools=no use_llvm=yes target=release bits=64"

    - name: macOS
      os: osx
      compiler: clang
      env: ADDITIONAL_FLAGS="" PLATFORM="osx"
      addons:
        homebrew:
          packages:
            - jq
            - p7zip

    - name: Windows
      os: windows
      env: ADDITIONAL_FLAGS="" PLATFORM="windows"

    - name: Android ARM
      os: linux
      env: ADDITIONAL_FLAGS="android_arch=armv7 target=release" PLATFORM="android"
      addons:
        apt:
          sources:
            - llvm-toolchain-xenial-6.0
          packages:
            [scons, pkg-config, build-essential, p7zip-full, clang-format-6.0]

    - name: Android ARM64
      os: linux
      env: ADDITIONAL_FLAGS="android_arch=arm64v8 target=release" PLATFORM="android"
      addons:
        apt:
          sources:
            - llvm-toolchain-xenial-6.0
          packages:
            [scons, pkg-config, build-essential, p7zip-full, clang-format-6.0]

    - name: iOS
      os: osx
      osx_image: xcode10.2
      compiler: clang
      env: ADDITIONAL_FLAGS="target=release" PLATFORM="iphone"
      addons:
        homebrew:
          packages:
            - jq
            - p7zip

install:
  - if [[ "$PLATFORM" == "x11" ]]; then
    sudo apt-get install -y build-essential scons pkg-config libx11-dev libxcursor-dev libxinerama-dev libgl1-mesa-dev libglu-dev libasound2-dev libpulse-dev libfreetype6-dev libssl-dev libudev-dev libxi-dev libxrandr-dev yasm;
    fi

  - if [[ "$PLATFORM" == "android" ]]; then
    cd .. && sh godot-fmod-integration/.travis/download_android_sdk.sh && cd godot-fmod-integration;
    fi

  - rm -rf api
  - mv .travis/download-fmod.sh ./ && chmod +x download-fmod.sh && ./download-fmod.sh $FMODUSER $FMODPASS $PLATFORM
  - if [[ "$PLATFORM" == "x11" ]]; then tar -xvf fmodstudioapi20001linux.tar.gz; fi
  - if [[ "$PLATFORM" == "osx" ]]; then hdiutil attach fmodstudioapi20001osx.dmg; fi
  - if [[ "$PLATFORM" == "windows" ]]; then
    7z x fmodstudioapi20001win-installer.exe;
    mv api/core/lib/x64/fmod_vc.lib api/core/lib/x64/fmod64_vc.windows.tools.64.lib;
    mv api/studio/lib/x64/fmodstudio_vc.lib api/studio/lib/x64/fmodstudio64_vc.windows.tools.64.lib;
    fi
  - if [[ "$PLATFORM" == "iphone" ]]; then hdiutil attach fmodstudioapi20001ios.dmg; fi
  - if [[ "$PLATFORM" == "android" ]]; then tar -xvf fmodstudioapi20001android.tar.gz; fi
  - if [[ "$PLATFORM" == "x11" ]]; then mv fmodstudioapi20001linux/api ./; fi
  - if [[ "$PLATFORM" == "osx" ]]; then cp -r "/Volumes/FMOD Programmers API Mac/FMOD Programmers API/api" ./; fi
  - if [[ "$PLATFORM" == "iphone" ]]; then cp -r "/Volumes/FMOD Programmers API iOS/FMOD Programmers API/api" ./; fi
  - if [[ "$PLATFORM" == "android" ]]; then mv fmodstudioapi20001android/api ./; fi

  - cd .. && git clone https://github.com/godotengine/godot.git && cd godot && git checkout 3.1.1-stable
  - mv ../godot-fmod-integration modules/fmod

  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then
    brew update;
    brew install scons p7zip;
    fi

  - if [[ "$TRAVIS_OS_NAME" == "windows" ]]; then
    curl -LO https://downloads.sourceforge.net/project/scons/scons-local/3.0.5/scons-local-3.0.5.zip;
    unzip scons-local-3.0.5.zip;
    fi

script:
  - if [[ "$TRAVIS_OS_NAME" == "windows" ]]; then
    export SCONS="./scons.bat";
    else
    export SCONS="scons";
    fi

  - $SCONS platform=$PLATFORM $ADDITIONAL_FLAGS;

  - if [[ "$STATIC_CHECKS" == "yes" ]]; then
    sh ./misc/travis/clang-format.sh;
    fi

  - if [[ "$ADDITIONAL_FLAGS" == "android_arch=arm64v8 target=release" ]]; then
    cp platform/android/java/libs/release/arm64-v8a/libgodot_android.so bin/libgodot.android.opt.armv8.so;
    fi

  - if [[  "$ADDITIONAL_FLAGS" == "android_arch=armv7 target=release" ]]; then
    cp platform/android/java/libs/release/armeabi-v7a/libgodot_android.so bin/libgodot.android.opt.armv7.neon.so;
    fi

deploy:
  provider: releases
  api_key:
    secure: $TRAVIS_TOKEN
  file:
    - "bin/godot.windows.tools.64.exe"
    - "bin/godot.x11.opt.64.llvm"
    - "bin/godot.osx.tools.64"
    - "bin/libgodot.android.opt.armv8.so"
    - "bin/libgodot.android.opt.armv7.neon.so"
    - "bin/libgodot.iphone.opt.arm64.a"
  skip_cleanup: true
  on:
    tags: true